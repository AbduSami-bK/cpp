# C++ Template

A C++ project template

## Build

```bash
cmake -S . -B build
cmake --build build
```

## Run

```bash
./build/main \
	--config configs/config.json \
	--enable-api \
	--log-level debug
```
